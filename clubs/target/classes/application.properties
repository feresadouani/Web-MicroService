# Bootstrap minimal : le reste vient du config server (configuration/clubs-service.properties)
spring.application.name=clubs-service
spring.config.import=optional:configserver:http://localhost:8082
server.port=8086