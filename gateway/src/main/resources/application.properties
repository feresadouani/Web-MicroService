# Bootstrap minimal : le reste vient du config server (classpath:configuration/gateway.properties)
spring.application.name=gateway
server.port=8081
spring.config.import=optional:configserver:http://localhost:8082
